# pypdf's floor below is a security minimum (from `pip-audit`), not a feature
# requirement -- transitive deps (pillow, click, requests, urllib3, pyarrow,
# etc.) are intentionally left unpinned; see ci.yml's pip-audit step for why.
# streamlit currently has open pip-audit findings (PYSEC-2026-212,
# PYSEC-2026-2285) with no released fix on PyPI yet as of 2026-08 (latest is
# 1.50.0) -- there's no version to pin to yet. Re-run `pip-audit` periodically
# and bump the floor once a fixed release ships.
streamlit
pandas
plotly
pypdf>=3.9.0
openpyxl
anthropic
